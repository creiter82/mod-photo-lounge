<div class="container">

<h1><%= @event.title %></h1>

<div>
  <div class="row">
    <% @event.event_photos.each do |photo| %>
      <div class="col-md-4">
        <%= image_tag photo, width: '100%' %>
      </div>
    <% end %>
  </div>
</div>

</div>